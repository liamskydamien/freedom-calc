import { InvestmentWeights } from "../../models/pof/InvestmentWeights";

/**
 * The investment allocation is the percentage of the total investment that is allocated to each asset group.
 */
export const INVESTMENT_ALLOCATION: InvestmentWeights = new InvestmentWeights(
  0.1,
  0.4,
  0.1,
  0.3,
  0.1,
  0.0,
);
