version: '3'
services:
  frontend:
    build:
      context: ./freedom-calc-frontend
      dockerfile: Dockerfile
    ports:
      - "8080:80"
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
